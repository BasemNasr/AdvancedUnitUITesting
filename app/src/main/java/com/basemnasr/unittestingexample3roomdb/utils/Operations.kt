package com.basemnasr.unittestingexample3roomdb.utils

object Operations {
    fun add(x: Int, y: Int): Int = x + y
    fun subtract(x: Int, y: Int): Int = x - y
    fun multiply(x: Int, y: Int): Int = x * y
    fun divide(x: Int, y: Int): Int = x / y
    fun notReturnedFun(x: Int){
        // do some thing
    }
}
